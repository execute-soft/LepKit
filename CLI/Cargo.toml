[package]
name = "lepkit"
version = "0.1.0"
edition = "2024"
description = "LepKit — scaffold a Leptos app from the leptos-setter template"

[[bin]]
name = "lepkit"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }